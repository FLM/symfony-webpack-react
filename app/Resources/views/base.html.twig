<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Hello world!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}
            {% if app.environment == 'dev' %}
                <script src="http://127.0.0.1:3100/static/client.js"></script>
            {% else %}
                <link rel="stylesheet" type="text/css" href="{{ asset('dist/client.css') }}">
                <script src="{{ asset('dist/client.js') }}"></script>
            {% endif %}
        {% endblock %}
    </body>
</html>
